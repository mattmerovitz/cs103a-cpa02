<div class="jumbotron">

    <p> Here is your list of inventory! </p>

      <table class="table table-bordered table-striped">
     <thead>

       <tr>
         <th>Item</th>
         <th>Size</th>
         <th>SKU</th>
         <th>Cost</th>
         <th>Mark as sold</th>
         <th>Remove item</th>
       </tr>

     </thead>
     <tbody>
        <%
        let total = 0
        let count = 0
         items.forEach(item => {
           total += item.cost
           count++
           %>
       <tr>
        <td><%= item.item %></td>
        <td><%= item.size %></td>
        <td><%= item.sku %></td>
        <td>$<%= item.cost.toFixed(2) %></td>
        <td><form method="post" action="sold">
          <input type="hidden" name="name" value="<%= item.item %>">
          <input type="hidden" name="sku" value="<%= item.sku %>">
          <input type="hidden" name="cost" value="<%= item.cost %>">
          <input type="hidden" name="size" value="<%= item.size %>">
          <input type="hidden" name="id" value="<%=item._id %>">
          <input type="number" name="soldprice" min="0.01" step="0.01" placeholder="Selling price" size="10" required>
          <input type="submit" value="Mark Sold">
        </form></td>
        <td><a href="/todo/remove/<%=item._id %>">Remove item</a></td>
       <% }) %>

     </tbody>
   </table>
   Your total cost is $<%= total.toFixed(2) %><br>
      <li><a href="/todo">Add more items to your inventory</a></li>
      <li><a href="/sold">View sold items</a></li>

</div>
